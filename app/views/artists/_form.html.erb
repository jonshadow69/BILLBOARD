<%= form_with(model: @artist, local: true, class: "ui form") do |f| %>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name, required: true, placeholder: "Name" %>
    </div>
    <div class="field">
      <%= f.label :genre %>
      <%= f.text_field :genre, required: true, placeholder: "genre" %>
    </div>
    <div class="area">
      <%= f.label :bio %>
      <%= f.text_area :bio, required: true, placeholder: "bio" %>
    </div>
     <%=f.submit%>
  <% end %>